name: Daily Tona Totka Generator

on:
  schedule:
    - cron: '30 18 * * *' # Runs daily at 18:30 UTC (12:00 AM IST)
  workflow_dispatch: # Allows manual trigger

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write # Needed to commit files back

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Content
        run: npx tsx scripts/generate-daily.ts
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/posts/ public/audio/ public/images/
          
          # Only commit if there are changes
          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          
          git commit -m "feat: daily tona totka"
          git push

      - name: Trigger Vercel Deployment
        if: success()
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK_URL }}
